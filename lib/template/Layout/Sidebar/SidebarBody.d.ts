import { ReactNode } from 'react';
interface IProps {
    children?: ReactNode;
    className?: string;
}
export declare const SidebarBody: ({ children, className }: IProps) => JSX.Element;
export {};
