import { ReactNode } from 'react';
interface IProps {
    className?: string;
    children?: ReactNode;
}
export declare const SidebarHeader: ({ className, children }: IProps) => JSX.Element;
export {};
