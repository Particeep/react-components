import { ReactNode } from 'react';
interface IProps {
    children?: ReactNode;
    className?: string;
}
export declare const SidebarFooter: ({ children, className }: IProps) => JSX.Element;
export {};
